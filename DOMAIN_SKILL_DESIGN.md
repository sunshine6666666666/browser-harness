# Browser Harness Domain Skill 设计范式

本文定义适用于任意网站的 Domain Skill 设计边界。`AGENTS.md` 已引用本文：在
`agent-workspace/domain-skills/` 下新建或修改任何 Domain Skill 之前必须先阅读本文。

## 1. 依据与样本边界

本规范归纳自 Browser Harness 上游仓库 `browser-use/browser-harness` 的 Domain Skills，而不是从本地扩展反推规则。

- 上游提交：`afbcc381b963040c19627d788e40c7e7663171ee`
- 上游目录：[agent-workspace/domain-skills](https://github.com/browser-use/browser-harness/tree/afbcc381b963040c19627d788e40c7e7663171ee/agent-workspace/domain-skills)
- 样本规模：95 个 Domain Skill 目录、107 份 Markdown、2 个 Python 文件。
- 107 份 Markdown 中：103 份记录 URL，104 份包含可执行片段，100 份记录陷阱或禁止事项，84 份包含明确验证方式。
- Markdown 中位长度约 309 行；长度不是目标，覆盖真实网站契约才是目标。

本地存在但上游没有的 ChatGPT、Gemini、Google、微博、知乎、喜马拉雅、Bilibili 发布等扩展不参与本规范归纳。对于本地与上游同名但内容已修改的文件，以精确上游提交中的版本为依据。

小红书需要特别说明：上游当前确实包含 `xiaohongshu/scraping.md`，因此上游版本属于官方样本；本地修改和 `xiaohongshu-humanized` 不作为官方样本。

代表性样本：

- `arxiv/scraping.md`：优先公开 API，明确“不使用浏览器”。
- `articulate-rise/code-blocks.md`：记录 Frame、sandbox、编辑器和真实验证陷阱。
- `gmail/compose.md`：描述一个写操作的必要交互、作用域和成功信号。
- `github/repo-actions.md`：以登录前置条件、单次状态变更和后置状态验证描述 Star/Watch。
- `tiktok/upload.md`：给出上传、排期和提交配方，但不建设通用上传框架。
- `shopify-admin/polaris-inputs.md`：保存框架输入机制和最小可复用函数。
- `tasksquad-ai/tasks.md`：记录页面状态、业务枚举、动作配方和异步陷阱。
- `xiaohongshu/scraping.md`：保留直接 URL、筛选、重复 DOM 节点和结果验证知识。

## 2. 核心定义

### 2.1 Domain Skill 是什么

Domain Skill 是面向一个网站或紧密相关域名的**可执行领域操作手册（Executable Domain Playbook）**。

它保存三类昂贵且可复用的知识：

1. 网站真实结构：URL、页面状态、Frame、DOM、API、数据模型和限制。
2. 已验证操作配方：达到一个业务状态所需的最短可靠动作序列。
3. 可靠性边界：前置条件、后置条件、同步信号、失败证据、重试安全和禁止事项。

它不应该成为：

- Browser Harness 底层能力的重复包装；
- 每个按钮一个函数的 Page Object；
- 把所有可能需求预先实现的站点 SDK；
- 一次任务的过程日志、截图索引或临时证据仓库；
- 强迫未来 Agent 走固定完整流程的宏脚本。

### 2.2 “原子”应当指什么

Domain Skill 中的原子性不是“一个点击”或“一次键盘输入”，而是**业务动作原子性（Business-action Atomicity）**：

> 一个动作完成一次可独立命名、可观察、可验证的业务读取或状态转换。

例如：

- 合适：`list_invitations()`、`select_album()`、`read_report_state()`、`submit_once()`。
- 太低：`click_filter_button()`、`focus_title_input()`、`press_enter_on_tag()`。
- 太高：`publish_everything_and_cleanup()`、`book_trip_from_search_to_payment()`。

底层点击、输入、滚动、文件设置和 CDP 调用已经由 Browser Harness 提供。Domain Skill 只在网站机制要求时组合它们。

## 3. 四层抽象模型

| 层级 | 名称 | 责任 | 归属 |
| --- | --- | --- | --- |
| L0 | 浏览器交互原语（Browser Interaction Primitives） | 导航、点击、输入、滚动、上传、JS、CDP、Tab/Frame 操作 | Browser Harness Core |
| L1 | 领域事实与适配知识（Domain Facts and Adaptation Knowledge） | URL、页面结构、稳定标识、API、Frame、限制、陷阱 | Domain Skill Markdown |
| L2 | 领域能力原语（Domain Capability Primitives） | 可独立验证的查询或业务状态转换 | Markdown 配方；必要时 Python |
| L3 | 工作流编排（Workflow Orchestration） | 根据用户目标组合多个 L2 能力、选择分支、恢复和收尾 | 当前 Agent、执行计划或任务脚本 |

Domain Skill 的主要边界是 L1 和 L2。L0 不重复实现，L3 默认不固化。

### 3.1 L0：复用 Browser Harness，不在 Domain Skill 重造

典型 L0 能力：

- `new_tab()`、`goto_url()`、`wait_for_load()`；
- `click_at_xy()`、`type_text()`、`press_key()`、`scroll()`；
- `upload_file()`；
- `js()`、`cdp()`；
- `iframe_target()`、`switch_tab()`、`close_tab()`；
- AX Tree、DOM、Network、Runtime 等协议能力。

Domain Skill 可以直接调用这些能力，但不要为了改名而包装。例如，只有一个调用者的 `click_button_by_text()` 通常不应成为公开函数。

### 3.2 L1：Domain Skill 必须保留的核心价值

L1 是官方样本最稳定的共同部分：

- 何时完全不需要浏览器，应该走公开 API、RSS、HTML 或内嵌状态；
- 规范化 URL 和可直接进入的深链接；
- 认证墙、登录态和权限边界；
- 顶层页面、iframe、Shadow DOM、虚拟列表和编辑器结构；
- 稳定标识与易失标识；
- 页面控件的真实语义及常见误判；
- 请求/响应字段、分页、速率限制和数据类型；
- 页面加载后的真实业务就绪条件；
- 已验证失败路径、反爬或框架陷阱；
- 成功状态如何观测；
- 信息的验证日期和适用 UI 变体。

即使没有任何 Python 文件，只要这些知识能让 Agent 不再重新摸索，Domain Skill 就已经成立。

### 3.3 L2：推荐的函数粒度

一个公开函数应当满足以下条件：

1. 名称表达业务能力，而不是页面动作。
2. 有明确输入、结构化输出和错误语义。
3. 有可观察的前置条件和后置条件。
4. 可以单独调用、单独验证、单独失败。
5. 屏蔽的是网站特有复杂性，而不是普通的 Browser Harness 调用。
6. 不暗中完成调用者没有要求的下游业务动作。

适合成为 L2 公开函数的类别：

#### A. 只读发现与查询

- 读取当前账号或工作区身份；
- 列出业务对象；
- 按稳定 ID 获取单个对象；
- 读取页面、表单或任务当前状态；
- 提取分页结果；
- 获取网站限制或能力矩阵。

示意名称：

```python
account_identity()
list_albums()
get_track(track_id)
read_report_state()
search_posts(query, sort)
```

#### B. 有明确读回的局部状态设置

- 选择一个确切对象；
- 设置一个业务字段或一组不可分割的关联字段；
- 切换一个业务模式；
- 上传一个文件并等待网站确认接收。

示意名称：

```python
select_album(album_id)
set_schedule_datetime(value)
set_visibility(mode)
upload_audio(path)
```

“局部”的判断标准是：动作完成后，调用者仍能检查状态、改变其他字段或放弃提交。

#### C. 非幂等提交边界

发送、发布、购买、删除、替换、邀请等动作应作为一个受保护的业务命令整体封装，而不是把危险点击裸露给编排层。

示意名称：

```python
submit_once(expected_state)
send_once(message)
delete_once(exact_record, confirm=True)
replace_once(exact_record, file, confirm=True)
```

此类函数必须负责：

- 动作前重新核验身份和目标；
- 校验完整前置状态；
- 最多激活一次业务副作用；
- 在等待前记录“已激活”事实；
- 只读核验最终状态；
- 结果未知时返回不可重试状态；
- 不把超时解释成失败并自动重放。

#### D. 复杂浏览上下文适配

只有 Frame、Shadow DOM、Canvas、富文本编辑器、虚拟列表或受控输入确实形成重复障碍时，才提供专用适配函数。它们一般是私有函数；只有多个业务能力都需要调用时才考虑公开。

## 4. Markdown 与 Python 的边界

### 4.1 默认从 Markdown 开始

上游 109 个有效文件中有 107 个 Markdown、仅 2 个 Python。这说明默认选择是：

> 先记录稳定事实和可复制配方；仅在文字与短片段不足以保证复用和安全时写代码。

Markdown 适合：

- URL 与访问路径；
- API 优先还是浏览器优先的决策；
- 页面/Frame/DOM 结构；
- 字段、限制和状态枚举；
- 10～30 行以内的典型操作片段；
- 验证方式；
- 陷阱、降级路径和禁止事项；
- 少量按场景组合的工作流配方。

### 4.2 只有满足准入条件才增加 Python

增加 Python 文件至少应满足一项：

- 同一段非平凡逻辑会被多个任务重复调用；
- 网站状态解析存在循环、分页、树遍历或复杂归一化；
- 非幂等操作需要统一的单次提交和未知结果保护；
- 多个调用点必须共享同一身份、权限或目标守卫；
- 短片段很容易被复制错，且错误会造成真实副作用；
- 需要结构化返回值供后续 Agent 判断，而不是仅打印文本。

不应因为以下理由增加 Python：

- 让 Markdown 看起来更短；
- 给一个 CSS selector 换名字；
- 为一次性任务预留未来扩展；
- 把普通 `js()`、`click_at_xy()` 或 `upload_file()` 包一层；
- 把所有页面字段预先做成 setter；
- 单纯追求“像 SDK”。

### 4.3 公开与私有接口

- 公开函数：Agent 可以理解并主动选择的 L2 业务能力。
- 私有函数：选择器、坐标、DOM 遍历、事件分发、序列化和等待细节。
- Markdown：解释公开函数何时使用、何时不能使用，以及不用函数时的最短配方。

公开 API 数量没有硬性上限，但每增加一个都必须对应一个独立用户意图。若两个函数只能固定成对调用，应考虑合并；若一个函数包含多个可单独取消或变更的意图，应考虑拆分。

## 5. Domain Skill Markdown 推荐结构

只写与该网站有关的段落，不为套模板制造空章节。

### 5.1 适用范围与最快路径

- 这个 Skill 解决哪些任务；
- 明确不解决什么；
- 公开 HTTP/API、浏览器 UI、混合 UI/API 三条路径如何选择；
- 如果不用浏览器就能完成，第一行直接说明。

### 5.2 URL 与状态地图

- 规范 URL、深链接和重定向；
- 登录前后 URL；
- 列表、详情、编辑、创建、管理页面；
- URL 中稳定 ID 的语义；
- 不可靠的标题、索引或临时参数。

### 5.3 认证、权限与隔离

- 是否需要登录；
- 合法会话复用方式；
- 账号、组织、工作区或店铺的核验方法；
- MFA、验证码、同意、账号选择出现时的停止边界；
- 禁止读取或记录的凭据。

### 5.4 页面与浏览上下文

- 顶层浏览上下文、Frame、Shadow DOM、Worker 或弹窗；
- 真正承载业务内容的 Document；
- SPA 水合和虚拟列表；
- 富文本编辑器、Canvas 或受控组件；
- 跨域限制及可用的 CDP 路径。

### 5.5 数据与业务状态

- 对象的稳定 ID；
- 关键字段和数据类型；
- 状态枚举及允许的转换；
- 分页、排序、游标和总数；
- 字符、时间、大小、格式、速率等限制。

### 5.6 已验证操作配方

每个配方围绕一个 L2 能力，包含：

1. 前置条件；
2. 最少必要动作；
3. 明确等待条件；
4. 后置状态验证；
5. 失败证据和安全恢复；
6. 已知不能使用的方法。

不要只给一串点击步骤。描述目标状态和因果关系。

### 5.7 稳定定位与交互策略

定位优先级：

1. 稳定业务 ID、URL 或服务端对象 ID；
2. Role、Accessible Name、Label 等语义定位；
3. 网站明确提供的 `data-testid` 或稳定属性；
4. 在确切父容器内作用域化的 DOM selector；
5. 文本与可见性组合；
6. 视觉定位或坐标作为最后降级。

记录为什么某种定位稳定，不要只堆 selector。每次导航、渲染、菜单或列表变化后重新解析目标。

### 5.8 同步与业务就绪

- `DOMContentLoaded`、`load` 和网络空闲不等于业务就绪；
- 等待具体元素、状态、响应、记录或 URL；
- 固定等待只用于短暂 UI settle，不能作为完成证明；
- 一个总体截止时间向内部步骤传播；
- 轮询必须有界并保留最后一次证据。

### 5.9 成功验证

按风险选择证据：

- 只读提取：结构化字段存在、数量合理、稳定 ID 可解析；
- 局部设置：控件值和应用状态准确读回；
- 创建/发送/发布：精确对象 ID、服务端列表/详情和 UI 后置状态；
- 删除/替换：目标身份一致、服务端最终状态明确、其他对象不变；
- 下载：新文件的路径、修改时间、大小和格式；
- 异步任务：任务 ID 和明确终态，而不是按钮消失。

截图只能证明视觉状态，不能单独证明服务端副作用。

### 5.10 陷阱、降级与禁止事项

优先记录这些高价值事实：

- 看起来有效但实际不触发框架状态的输入；
- 隐藏重复节点、错误的第一个匹配；
- iframe 层级、sandbox 和跨域限制；
- 页面文字与真实业务状态不同步；
- API、HTML、DOM 或内嵌状态的优先级；
- 反爬、速率限制和允许的降级路径；
- 非幂等动作为什么不能重试。

### 5.11 新鲜度与证据

- 记录验证日期、网站变体和账号类型，但不记录秘密；
- 将稳定结论写入 Skill；
- 将一次运行的 ID、标题、截图、请求样本和临时结果留在任务 Evidence，不写成永久规范；
- 旧结论与当前页面冲突时，以当前可验证事实为准并更新文档。

## 6. 函数设计规则

### 6.1 输入

- 使用业务对象 ID，不使用列表序号；
- 明确字符串归一化、时区、单位和空值语义；
- 文件在网页动作前完成本地存在性、大小和格式验证；
- destructive/write 函数要求精确目标和显式确认参数；
- 账号、浏览器、Profile、租户、店铺、组织和工作区属于运行时上下文，必须由调用任务传入或从当前会话读取后与调用参数核对；不得写成 Domain Skill 常量或默认值；
- 不通过函数默认值猜账号、店铺、专辑或工作区；永久示例使用参数名，真实身份只进入任务 Evidence；测试仅使用合成身份 fixture。

### 6.2 输出

优先返回结构化字典，而不是布尔值或自由文本：

```python
{
    "status": "verified",
    "object_id": "stable-id",
    "state": "published",
    "evidence_source": "manager_api",
    "attempts": 1,
    "retry": False,
}
```

输出必须区分：

- 动作未发生；
- 动作发生且成功；
- 动作发生但结果未知；
- 平台明确拒绝；
- 前置条件不成立。

### 6.3 错误

- 不要吞掉会改变业务判断的异常；
- 错误应指出阶段、预期、观测和是否安全重试；
- 目标缺失与目标重复是不同错误；
- 身份不符、权限不足、认证墙、平台拒绝和超时必须区分；
- 结果未知不能伪装成普通失败。

### 6.4 副作用

每个写函数都要回答：

- 具体改变什么；
- 最多触发几次；
- 从哪一刻开始不可安全重试；
- 如何确认服务端已接受；
- 如何在中断后只读恢复；
- 哪些相邻对象必须保持不变。

### 6.5 等待与重试

- 读取和幂等查询可以有界重试；
- 非幂等写操作默认不重试；
- 写操作之后的只读核验可以重试；
- 重试前先判断前一次是否已产生效果；
- 服务端支持幂等键时才能据此安全重发；浏览器本身不会自动提供幂等性。

## 7. 工作流编排边界

Markdown 可以给出完整工作流示例，但实现仍由 Agent 组合 L2 能力。

推荐：

```text
核验身份 → 查询目标 → 准备局部状态 → 读取提交快照
→ 单次提交 → 只读核验 → 根据结果决定结束或恢复
```

不推荐把它固化为一个无法介入的大函数：

```text
登录 → 选素材 → 填所有字段 → 发布 → 等待 → 删除 → 清理浏览器
```

原因：用户常常只需要其中一段；网站 UI 会出现分支；Agent 需要在提交前展示或检查状态；失败时需要从已验证检查点恢复。

允许整体封装的例外是单次非幂等边界。例如“最终提交并确认一次”可以是一个函数，因为拆开会增加重复点击风险。但准备表单、最终提交和事后清理仍应是三个可独立控制的能力。

## 8. 常见反模式

### 8.1 点击级公开 API

```python
open_menu()
click_latest()
press_save()
```

问题：暴露易变 UI 细节，没有业务后置条件。此类逻辑应是配方片段或私有 helper。

### 8.2 巨型端到端宏

```python
publish_and_verify_and_delete_everything(config)
```

问题：隐藏中间状态，难以暂停、审阅、恢复或复用局部能力。

### 8.3 为所有字段预造 setter

问题：把偶尔出现的 UI 变体固化成长期 API。只为当前任务必需或高频复用字段提供函数。

### 8.4 只验证点击，不验证结果

按钮可见、事件已派发、Toast 出现都不等于业务成功。必须验证目标状态。

### 8.5 把 API 调用称为“绕过浏览器”

合法调用公开或页面自身使用的接口应称为**接口级自动化（API-Level Automation）**或**混合 UI/API 工作流（Hybrid UI/API Workflow）**。只有规避访问控制时才是 bypass。

### 8.6 把任务 Evidence 写进永久 Skill

一次运行的账号、素材、对象 ID、标题、截图和临时文件路径会污染通用知识。Skill 记录稳定契约；Evidence 记录一次执行。

### 8.7 复制 Core helper

重复实现通用点击、输入、等待、上传、Tab 和 Frame helper 会产生行为分叉。先查 Browser Harness 已有能力。

## 9. 创建或更新 Domain Skill 的决策流程

```text
1. 普通 HTTP/API/RSS 能完成吗？
   ├─ 能：记录最短接口路径，避免浏览器。
   └─ 不能：进入真实浏览器页面。

2. page_info() 返回已有 Domain Skill 吗？
   ├─ 是：全部读取并复用，补充缺失事实。
   └─ 否：建立新的网站事实记录。

3. 当前发现是稳定、可复用、已验证的吗？
   ├─ 否：留在任务 Evidence，不写 Skill。
   └─ 是：写入 Markdown。

4. Markdown + 短代码片段足够可靠复用吗？
   ├─ 是：停止，不建 Python。
   └─ 否：识别一个 L2 业务能力。

5. 该能力是否有明确输入、输出、前后置条件和独立验收？
   ├─ 否：继续写配方，不公开成函数。
   └─ 是：实现最小函数。

6. 是否包含非幂等副作用？
   ├─ 是：加入身份、目标、单次激活、未知结果和只读恢复保护。
   └─ 否：使用有界等待和结构化返回。
```

## 10. 评审清单

### 文档

- [ ] 明确了适用范围和最快路径。
- [ ] 明确何时不用浏览器。
- [ ] URL、稳定 ID、页面/Frame 结构已验证。
- [ ] 字段、状态、分页和限制有准确语义。
- [ ] 每个配方都有前置条件、等待和后置验证。
- [ ] 记录了失败方法、陷阱和禁止事项。
- [ ] 登录态复用没有被写成认证绕过。
- [ ] 没有把一次运行的 Evidence 当成永久知识。

### 函数

- [ ] 每个公开函数对应一个独立用户意图。
- [ ] 函数名称表达业务能力而非点击细节。
- [ ] 普通 Browser Harness 原语没有被重复包装。
- [ ] 输入使用精确业务 ID，不依赖列表位置。
- [ ] 输出区分成功、拒绝、未发生和结果未知。
- [ ] 写操作有身份和目标守卫。
- [ ] 非幂等动作最多激活一次。
- [ ] 超时后没有盲目重放。
- [ ] 私有 helper 只承载网站特有机制。
- [ ] 没有为未出现的 UI 或未来需求预造扩展点。

### 验证

- [ ] 使用目标状态验证，而不是只看点击或 Toast。
- [ ] 异步等待基于具体条件且有截止时间。
- [ ] API、DOM 和视觉证据的能力边界明确。
- [ ] 删除、替换、发送、发布等动作验证相邻对象未变。
- [ ] 非平凡解析或状态分支有最小可运行测试。
- [ ] Domain Skill registry 和发现流程已验证。

## 11. 一句话规范

> Domain Skill 保存网站特有的稳定事实和可独立验证的业务能力；复用 Browser Harness 的底层原语，把跨能力的工作流编排留给 Agent，并把非幂等副作用收口在单次、可核验、不可盲目重放的业务命令中。
